# === SECRETS (NON-NEGOTIABLE) ===
# This is your #1 defense. Contains all API keys, passwords, etc.
# This MUST be here.
.env
*.env
*.env.local

# === DEPENDENCIES (THE "NOISE") ===
# Never commit dependencies. This is the cardinal sin of Node.js.
# This folder is rebuilt on any new machine with 'npm install'.
node_modules/

# === LOGS & DEBUG FILES ===
# These are noise and specific to your local machine.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# === OS-SPECIFIC JUNK (WINDOWS) ===
# This is machine-specific clutter from your Windows OS.
# It has no place in a clean repository.
Thumbs.db
desktop.ini
NTUSER.DAT
NTUSER.DAT.LOG*

# === COMMON IDE / EDITOR FILES ===
# Ignores settings for specific editors (like VS Code or JetBrains).
.vscode/
.idea/

# === COMMON BUILD ARTIFACTS ===
# Ignore production build folders. These are generated, not written.
dist/
build/
.next/
